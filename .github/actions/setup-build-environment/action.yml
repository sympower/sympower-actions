name: 'Setup Build Environment'
description: 'Setup OpenJDK, Checkout, Login to AWS ECR'
inputs:
  MAVEN_REPO_PUBLIC_URL:
    description: 'Maven dependency URL'
    required: true
  MAVEN_REPO_SNAPSHOTS_URL:
    description: 'Maven SNAPSHOT deployment URL'
    required: true
  MAVEN_REPO_RELEASES_URL:
    description: 'Maven RELEASE deployment URL'
    required: true
  MAVEN_REPO_USER:
    description: 'Maven repository username'
    required: true
  MAVEN_REPO_PASS:
    description: 'Maven repository password'
    required: true
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
      with:
        # Disabling shallow clone is recommended for improving relevancy of reporting
        fetch-depth: 0

    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
      with:
        gradle-home-cache-cleanup: true

    - name: Cache SonarCloud packages
      uses: actions/cache@v3
      with:
        path: ~/.sonar/cache
        key: ${{ runner.os }}-sonar
        restore-keys: ${{ runner.os }}-sonar

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Configure Gradle
      shell: bash
      run: |
        set -eu
        
        mkdir -p ~/.gradle
        CONF_FILE=~/.gradle/gradle.properties
        
        echo '# Gradle configuration' > "$CONF_FILE"
        
        echo sympowerMavenRepoPublicUrl=${{ inputs.MAVEN_REPO_PUBLIC_URL }} >> "$CONF_FILE"
        echo sympowerMavenRepoSnapshotsUrl=${{ inputs.MAVEN_REPO_SNAPSHOTS_URL }} >> "$CONF_FILE"
        echo sympowerMavenRepoReleasesUrl={{ inputs.MAVEN_REPO_RELEASES_URL }} >> "$CONF_FILE"
  
        echo sympowerMavenRepoUsername=${{ inputs.MAVEN_REPO_USER }} >> "$CONF_FILE"
        echo sympowerMavenRepoPassword=${{ inputs.MAVEN_REPO_PASS }} >> "$CONF_FILE"

